package gpj.singleton;

/**
 * 枚举模式
 * 实现简单
 * 枚举本身就是单例模式。由JVM从根本上提供保障，避免通过反射和反序列化的漏洞
 * 但没有延时加载
 * @author gpj
 *
 */
public enum Singleton5 {
	/**
	 * 定义了一个枚举的元素，它就代表了Singleton5的一个实例
	 */
	INSTANCE;

	public void otherOperation()
	{
		//功能处理
	}
}
